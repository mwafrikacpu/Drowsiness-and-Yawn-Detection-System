# Production Requirements for Railway Deployment
# Optimized for cloud deployment without problematic packages

# Core Django
asgiref==3.7.2
Django==4.2.9
djangorestframework==3.14.0
django-crispy-forms==2.1
django-environ==0.11.2
sqlparse==0.4.4

# Database
dj-database-url==2.1.0
psycopg2-binary==2.9.9

# Async & WebSockets
channels==4.0.0
channels-redis==4.2.0
django-redis==5.4.0
redis==5.0.3

# Computer Vision (lightweight alternatives)
opencv-python-headless==4.9.0.80  # Headless version for servers
mediapipe==0.10.8
numpy==1.24.4
scipy==1.10.1
Pillow==10.2.0
imutils==0.5.4

# Audio (for production - lightweight)
# pygame==2.5.2  # Commented out - not needed in production

# Production Server
gunicorn==21.2.0
whitenoise==6.6.0

# Utilities
requests==2.31.0
certifi==2023.11.17
charset-normalizer==3.3.2
click==8.1.7
idna==3.6
packaging==23.2
pytz==2023.3.post1
six==1.16.0
typing-extensions==4.9.0
urllib3==2.1.0

# Environment
python-dotenv==1.2.1

# Background Tasks (lightweight alternative)
celery==5.3.4

# Removed problematic packages:
# dlib==19.24.2  # Compilation issues
# face-recognition==1.3.0  # Depends on dlib
# face-recognition-models==0.3.0  # Depends on dlib
# backports.zoneinfo  # Python 3.11 compatibility issues
# scapy==2.5.0  # Not needed for web app
# secure-smtplib==0.1.1  # Not needed
# eye-game==0.1  # Not needed
# pygame==2.5.2  # Not needed in production server
# django-background-tasks==1.2.5  # Replace with Celery